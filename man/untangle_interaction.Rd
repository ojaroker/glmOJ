% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/untangle-interaction.R
\name{untangle_interaction}
\alias{untangle_interaction}
\title{Untangle a two-way interaction in a fitted model}
\usage{
untangle_interaction(
  model,
  interaction,
  average.over = NULL,
  standardized = FALSE,
  n = 100,
  overridePlot = FALSE
)
}
\arguments{
\item{model}{A fitted model. Either an object returned by \code{\link[=countGLM]{countGLM()}} or
one of the package's individual fitters (\code{\link[=poissonGLM]{poissonGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}},
\code{\link[=tweedieGLM]{tweedieGLM()}}, \code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}}, \code{\link[=zeroinflNegbinGLM]{zeroinflNegbinGLM()}},
\code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}}), or a raw \code{glm} / \code{glmmTMB} fit. For \code{countGLM}
objects the best-fitting component model is used and a message is
printed.}

\item{interaction}{The pair of interacting variables. Either a length-2
character vector (e.g. \code{c("x7", "x8")}) or a one-sided formula
(e.g. \code{~ x7:x8} or \code{~ x7*x8}).}

\item{average.over}{Character vector of \emph{categorical} predictor names that
may be averaged over by emmeans. Default \code{NULL} means do not average
over any categorical predictor; each level is reported separately.}

\item{standardized}{Logical; set \code{TRUE} when the continuous predictors
have been standardized to mean 0 and SD 1. In that case the low /
medium / high values used for continuous x continuous slopes are
\code{c(-1, 0, 1)} (standard deviations), and the interpretation text refers
to a "one-standard-deviation increase" rather than a "one-unit
increase". Default \code{FALSE}.}

\item{n}{Integer; number of equally-spaced points used for the continuous
predictor in the categorical x continuous emmeans grid backing the
plot. Default 100.}

\item{overridePlot}{Logical; when \code{TRUE}, the faceted plot in the
categorical x categorical and categorical x continuous cases is built
and returned even if the number of facets exceeds 8. Default \code{FALSE}.}
}
\value{
A list with class \code{"untangle_interaction"} whose contents depend
on the interaction type:
\describe{
\item{\code{type}}{One of \code{"categorical-categorical"},
\code{"categorical-continuous"}, \code{"continuous-continuous"}.}
\item{\code{variables}}{Character vector of the two predictor names.}
\item{\code{emmeans}}{(cat x cat) data frame with columns for each factor,
\code{Mean}, \code{SE}, \code{df}, \code{Lower}, \code{Upper}.}
\item{\code{plot}}{(cat x cat) faceted ggplot of estimated means with 95\%
error bars. \code{NULL} when the number of facets exceeds 8 and
\code{overridePlot = FALSE}.}
\item{\code{emtrends}}{(cat x cont) data frame with \code{Slope}, \code{SE}, \code{df},
\code{Lower}, \code{Upper} at each level of the factor.}
\item{\code{plot}}{(cat x cont) faceted ggplot of predicted responses vs
the continuous predictor, coloured by the factor, with a 95\%
confidence ribbon. \code{NULL} when the number of facets exceeds 8 and
\code{overridePlot = FALSE}.}
\item{\verb{emtrends_<v1>}, \verb{emtrends_<v2>}}{(cont x cont) data frames of
slopes of each predictor at low/medium/high values of the other.}
\item{\verb{johnson_neyman_<v1>_by_<v2>}, \verb{johnson_neyman_<v2>_by_<v1>}}{(cont
x cont) Johnson-Neyman objects from \code{\link[interactions:johnson_neyman]{interactions::johnson_neyman()}}.
\code{NULL} if the call failed (e.g. unsupported model class).}
\item{\code{interpretation}}{A plain-language description, also printed.}
}
}
\description{
Produces post-hoc marginal means and/or slopes that describe how two
interacting predictors jointly shape the fitted model. The behaviour
adapts to the types of the two variables:
\itemize{
\item \strong{categorical x categorical:} estimated marginal means
(\code{\link[emmeans:emmeans]{emmeans::emmeans()}}) for every combination of the two factors, plus
a faceted ggplot of those means with 95\% error bars. Faceting is by
any categorical predictor not listed in \code{average.over}. The plot is
suppressed (with a warning) when the number of facets exceeds 8,
unless \code{overridePlot = TRUE}.
\item \strong{categorical x continuous:} slopes of the continuous predictor at
each level of the factor (\code{\link[emmeans:emtrends]{emmeans::emtrends()}}), plus a faceted
ggplot of predicted responses across \code{n} equally-spaced values of the
continuous predictor. Faceting is by any categorical predictor not
listed in \code{average.over}. The plot is suppressed (with a warning) when
the number of facets exceeds 8, unless \code{overridePlot = TRUE}.
\item \strong{continuous x continuous:} slopes of each predictor at low,
medium and high values of the other (\code{\link[emmeans:emtrends]{emmeans::emtrends()}}), plus
Johnson-Neyman intervals in both directions
(\code{\link[interactions:johnson_neyman]{interactions::johnson_neyman()}}).
}
}
\details{
By default, the function never averages over \emph{categorical} predictors
outside the interaction of interest: results are reported separately for
each level. Predictors listed in \code{average.over} are averaged over instead.
Continuous predictors outside the interaction are always held at their
mean (the emmeans default) and therefore implicitly averaged in a linear
sense.

\code{emmeans} and \code{interactions} are declared in \code{Suggests}; both must be
installed before calling this function.

All emmeans summaries are returned on the response scale when a known
link is present. Slopes from \code{emtrends} are returned on the \strong{linear
predictor (link) scale} — on a log link, a positive slope means the
expected response grows multiplicatively with the predictor.
}
\examples{
\dontrun{
fit <- poissonGLM(y ~ x1 * x2 + x3, data = df)
untangle_interaction(fit, c("x1", "x2"))
untangle_interaction(fit, ~ x1:x2, average.over = "x3")
untangle_interaction(fit, c("x7", "x8"), standardized = TRUE)
}
}
\seealso{
\code{\link[=interpret_coef]{interpret_coef()}}, \code{\link[emmeans:emmeans]{emmeans::emmeans()}}, \code{\link[emmeans:emtrends]{emmeans::emtrends()}},
\code{\link[interactions:johnson_neyman]{interactions::johnson_neyman()}}
}
