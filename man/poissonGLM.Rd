% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poissonGLM.R
\name{poissonGLM}
\alias{poissonGLM}
\title{Fit a Poisson regression model}
\usage{
poissonGLM(formula, data, ...)
}
\arguments{
\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}). The response must be
a non-negative integer count variable.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{...}{Additional arguments passed to \code{\link[stats:glm]{stats::glm()}}.}
}
\value{
An object of class \code{c("poissonGLM", "countGLMfit")}, a list with:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{model}}{The underlying \link[stats:glm]{stats::glm} fit object.}
\item{\code{summary}}{The result of \code{summary()} on the fitted model.}
\item{\code{coefficients}}{A data frame with columns \code{term}, \code{exp.coef},
\code{lower.95}, \code{upper.95} (all on the response/exponentiated scale).}
\item{\code{diagnostics}}{A list with:
\describe{
\item{\code{rqr}}{Numeric vector of randomized quantile residuals.}
\item{\code{dispersion_ratio}}{Pearson chi-squared / df.residual.
Values substantially above 1 (rule of thumb: > 1.5) suggest
overdispersion; consider \code{\link[=negbinGLM]{negbinGLM()}}.}
\item{\code{plot}}{A patchwork ggplot: fitted values vs RQR (left) and
histo-QQ of RQR (right). The dispersion ratio is shown in red with
an overdispersion warning if it exceeds 1.2.}
\item{\code{r2_plot}}{Squared Pearson residuals vs fitted values, with a
red dotted reference line at 1 and a smooth. Useful for diagnosing
mean-variance misspecification.}
}
}
\item{\code{aic}}{AIC of the fitted model.}
\item{\code{bic}}{BIC of the fitted model.}
}
}
\description{
Fits a Poisson GLM and returns model coefficients on the response scale
(exponentiated), randomized quantile residuals (RQR), a Pearson
dispersion ratio, and a two-panel diagnostic plot.
}
\details{
\strong{Coefficient interpretation:} Poisson regression models the log of the
expected count. Exponentiating a coefficient gives the multiplicative change in the expected count for a
one-unit increase in the predictor, adjusting for simultaneous linear changes in other predictors.
For example, 1.5 means a 50\% higher expected count.

\strong{Condition checking:} Inspect \code{diagnostics$dispersion_ratio}. A value
near 1 is consistent with the Poisson assumption (mean = variance). The
RQR diagnostic plot should show points scattered randomly around zero with
approximately normal QQ behaviour.
}
\examples{
df <- data.frame(
  y  = c(0L, 1L, 2L, 3L, 5L, 0L, 2L, 4L, 1L, 3L),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
fit <- poissonGLM(y ~ x1, data = df)
print(fit)
plot(fit)

}
\seealso{
\code{\link[=negbinGLM]{negbinGLM()}}, \code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}}, \code{\link[=countGLM]{countGLM()}}, \code{\link[stats:glm]{stats::glm()}}
}
