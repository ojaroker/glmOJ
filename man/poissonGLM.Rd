% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poissonGLM.R
\name{poissonGLM}
\alias{poissonGLM}
\title{Fit a Poisson regression model}
\usage{
poissonGLM(
  formula,
  data,
  assessZeroInflation = TRUE,
  maxit = NULL,
  dispersion_threshold = 1.2,
  ...
)
}
\arguments{
\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}). The response must be
a non-negative integer count variable.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{assessZeroInflation}{Logical; when \code{TRUE} (default), runs a DHARMa
simulation-based zero-inflation test after fitting. Issues a warning if
significant zero-inflation is detected and adds \code{zi_test} to the returned
diagnostics. Set to \code{FALSE} when calling from \code{\link[=countGLM]{countGLM()}}, which performs
its own zero-inflation assessment.}

\item{maxit}{Optional integer; maximum IWLS iterations passed through as
\code{control = stats::glm.control(maxit = maxit)}. Ignored when the user
supplies their own \code{control} via \code{...}.}

\item{dispersion_threshold}{Numeric; dispersion ratios above this value
are flagged as overdispersed in the diagnostic plot. Default 1.2.}

\item{...}{Additional arguments passed to \code{\link[stats:glm]{stats::glm()}}.}
}
\value{
An object of class \code{c("poissonGLM", "countGLMfit")}, a list with:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{model}}{The underlying \link[stats:glm]{stats::glm} fit object.}
\item{\code{summary}}{The result of \code{summary()} on the fitted model.}
\item{\code{coefficients}}{A data frame with columns \code{term}, \code{exp.coef},
\code{lower.95}, \code{upper.95} (all on the response/exponentiated scale).}
\item{\code{diagnostics}}{A list with:
\describe{
\item{\code{rqr}}{Numeric vector of randomized quantile residuals.}
\item{\code{dispersion_ratio}}{Pearson chi-squared / df.residual.}
\item{\code{plot}}{Patchwork ggplot: fitted vs RQR and histo-QQ.}
\item{\code{r2_plot}}{Squared Pearson residuals vs fitted values.}
\item{\code{zi_test}}{When \code{assessZeroInflation = TRUE}, a list with
\code{detected} (logical), \code{p_value} (numeric), and \code{plot} (ggplot
histogram of DHARMa simulated zero proportions vs observed).
\code{NULL} when \code{assessZeroInflation = FALSE}.}
}
}
\item{\code{aic}}{AIC of the fitted model.}
\item{\code{bic}}{BIC of the fitted model.}
}
}
\description{
Fits a Poisson GLM and returns model coefficients on the response scale
(exponentiated), randomized quantile residuals (RQR), a Pearson
dispersion ratio, and diagnostic plots.
}
\details{
\strong{Coefficient interpretation:} Poisson regression models the log of the
expected count. Exponentiating a coefficient gives the multiplicative change
in the expected count for a one-unit increase in the predictor.

\strong{Condition checking:} Inspect \code{diagnostics$dispersion_ratio} (near 1 is
good) and \code{diagnostics$zi_test$p_value} for zero-inflation.
}
\examples{
df <- data.frame(
  y  = c(0L, 1L, 2L, 3L, 5L, 0L, 2L, 4L, 1L, 3L),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
fit <- poissonGLM(y ~ x1, data = df)
print(fit)
plot(fit)

}
\seealso{
\code{\link[=negbinGLM]{negbinGLM()}}, \code{\link[=tweedieGLM]{tweedieGLM()}}, \code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}},
\code{\link[=countGLM]{countGLM()}}, \code{\link[stats:glm]{stats::glm()}}
}
