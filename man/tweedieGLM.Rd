% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tweedieGLM.R
\name{tweedieGLM}
\alias{tweedieGLM}
\title{Fit a Tweedie regression model}
\usage{
tweedieGLM(formula, data, assessZeroInflation = TRUE, maxit = NULL, ...)
}
\arguments{
\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}). The response must be
non-negative.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{assessZeroInflation}{Logical; when \code{TRUE} (default), runs a DHARMa
simulation-based zero-inflation test after fitting. Issues a warning if
significant zero-inflation is detected and adds \code{zi_test} to the returned
diagnostics. Set to \code{FALSE} when calling from \code{\link[=countGLM]{countGLM()}}, which performs
its own zero-inflation assessment.}

\item{maxit}{Optional integer; maximum optimizer iterations passed through
as \code{control = glmmTMB::glmmTMBControl(optCtrl = list(iter.max = maxit, eval.max = maxit))}. Ignored when the user supplies their own \code{control}
via \code{...}.}

\item{...}{Additional arguments passed to \code{\link[glmmTMB:glmmTMB]{glmmTMB::glmmTMB()}}.}
}
\value{
An object of class \code{c("tweedieGLM", "countGLMfit")}, a list with:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{model}}{The underlying \link[glmmTMB:glmmTMB]{glmmTMB::glmmTMB} fit object.}
\item{\code{summary}}{The result of \code{summary()} on the fitted model.}
\item{\code{phi}}{The estimated Tweedie dispersion parameter (phi).}
\item{\code{p}}{The estimated Tweedie power parameter (p), restricted to
(1, 2). Values near 1 resemble Poisson; values near 2 resemble Gamma.
\code{NA} if the parameter cannot be extracted from the fit.}
\item{\code{coefficients}}{A data frame with columns \code{term}, \code{exp.coef},
\code{lower.95}, \code{upper.95}, \code{p.value}, and \code{stars} (all on the
response/exponentiated scale).}
\item{\code{diagnostics}}{A list with:
\describe{
\item{\code{rqr}}{Numeric vector of randomized quantile residuals
(DHARMa simulation-based, converted to normal scale).}
\item{\code{dispersion_ratio}}{Pearson chi-squared / df.residual.}
\item{\code{plot}}{Patchwork ggplot: fitted vs RQR and histo-QQ.}
\item{\code{r2_plot}}{Squared Pearson residuals vs fitted values.}
\item{\code{zi_test}}{When \code{assessZeroInflation = TRUE}, a list with
\code{detected} (logical), \code{p_value} (numeric), and \code{plot} (ggplot
histogram of DHARMa simulated zero proportions vs observed).
\code{NULL} when \code{assessZeroInflation = FALSE}.}
}
}
\item{\code{aic}}{AIC of the fitted model.}
\item{\code{bic}}{BIC of the fitted model.}
}
}
\description{
Fits a Tweedie GLM (via \code{\link[glmmTMB:glmmTMB]{glmmTMB::glmmTMB()}}) and returns model coefficients
on the response scale (exponentiated), randomized quantile residuals (RQR),
estimated dispersion (\code{phi}) and power (\code{p}) parameters, and diagnostic
plots. The Tweedie family generalises Poisson and Gamma distributions and is
well-suited to non-negative count data with complex variance structures.
}
\details{
\strong{Three parameters:} The Tweedie family is characterised by three estimated
quantities: the regression coefficients (mean structure, via a log link),
the dispersion \code{phi} (Var(Y) = phi * mu^p), and the power \code{p} (1 < p < 2).
When \code{p} is close to 1, the variance structure resembles Poisson; when
close to 2, it resembles Gamma.

\strong{Coefficient interpretation:} Exponentiating a coefficient gives the
multiplicative change in the expected response for a one-unit increase in
the predictor, holding all other predictors constant.

\strong{When to use:} Tweedie regression is appropriate for non-negative count
data with complex variance structures not well captured by Poisson or
negative binomial. If zero-inflation is also present, consider
\code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}}.
}
\examples{
df <- data.frame(
  y  = c(0, 0, 1.5, 3.2, 5.8, 0, 0.9, 4.1, 0, 2.7),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
fit <- suppressWarnings(tweedieGLM(y ~ x1, data = df))
print(fit)
plot(fit)

}
\seealso{
\code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}}, \code{\link[=poissonGLM]{poissonGLM()}},
\code{\link[=countGLM]{countGLM()}}, \code{\link[glmmTMB:glmmTMB]{glmmTMB::glmmTMB()}}
}
