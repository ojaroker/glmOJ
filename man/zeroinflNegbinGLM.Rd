% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zeroinflNegbinGLM.R
\name{zeroinflNegbinGLM}
\alias{zeroinflNegbinGLM}
\title{Fit a zero-inflated negative binomial regression model}
\usage{
zeroinflNegbinGLM(
  formula,
  data,
  ziformula = NULL,
  maxit = NULL,
  dispersion_threshold = 1.2,
  ...
)
}
\arguments{
\item{formula}{A model formula for the \strong{count} component (e.g.
\code{y ~ x1 + x2}). The response must be a non-negative integer count.}

\item{data}{A data frame containing the variables in \code{formula} (and
\code{ziformula} if provided).}

\item{ziformula}{A one-sided formula for the \strong{zero-inflation} component
(e.g. \code{~ x1}). When \code{NULL} (default), the same right-hand side as
\code{formula} is used for both components. Use \code{~ 1} for an intercept-only
zero-inflation model.}

\item{maxit}{Optional integer; maximum optimizer iterations passed through
as \code{control = pscl::zeroinfl.control(maxit = maxit)}. Ignored when the
user supplies their own \code{control} via \code{...}.}

\item{dispersion_threshold}{Numeric; dispersion ratios above this value
are flagged as overdispersed in the diagnostic plot. Default 1.2.}

\item{...}{Additional arguments passed to \code{\link[pscl:zeroinfl]{pscl::zeroinfl()}}.}
}
\value{
An object of class \code{c("zeroinflNegbinGLM", "zeroinflGLMfit", "countGLMfit")}, a list with:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{model}}{The underlying \link[pscl:zeroinfl]{pscl::zeroinfl} fit object.}
\item{\code{theta}}{The estimated negative binomial dispersion parameter.}
\item{\code{coefficients}}{A list with two data frames, each with columns
\code{term}, \code{exp.coef}, \code{lower.95}, \code{upper.95}:
\describe{
\item{\code{count}}{Exponentiated coefficients for the count component.}
\item{\code{zero}}{Exponentiated coefficients for the zero-inflation
component.}
}
}
\item{\code{summary}}{The result of \code{summary()} on the fitted model.}
\item{\code{diagnostics}}{A list with \code{rqr}, \code{dispersion_ratio}, and
\code{plot} (see \code{\link[=poissonGLM]{poissonGLM()}} for details).}
\item{\code{aic}}{AIC of the fitted model.}
\item{\code{bic}}{BIC of the fitted model.}
}
}
\description{
Fits a zero-inflated negative binomial model (via \code{\link[pscl:zeroinfl]{pscl::zeroinfl()}}) with
separate count and zero-inflation components. Returns coefficients on the
response scale, randomized quantile residuals, a dispersion ratio, and a
diagnostic plot.
}
\details{
\strong{Coefficient interpretation:}
\itemize{
\item \emph{Count component}: exponentiating a coefficient gives the multiplicative
change in the expected count among non-structural-zero observations, for a
one-unit increase in the predictor, adjusting for simultaneous linear
changes in other predictors. For example, 1.5 means a 50\% higher expected
count.
\item \emph{Zero component}: exponentiating a coefficient gives the multiplicative
change in the odds of being a structural zero (vs. entering the count
process) for a one-unit increase in the predictor.
}

\strong{When to use:} Zero-inflated negative binomial handles both excess zeros
\emph{and} overdispersion in the non-zero counts. Prefer this over
\code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}} when the non-zero counts remain overdispersed. For
count data with complex variance structures and excess zeros, consider
\code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}}.
}
\examples{
df <- data.frame(
  y  = c(0L, 0L, 0L, 1L, 2L, 0L, 3L, 0L, 1L, 0L),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
fit <- zeroinflNegbinGLM(y ~ x1, data = df)
print(fit)
plot(fit)

# Intercept-only zero component:
fit2 <- zeroinflNegbinGLM(y ~ x1, data = df, ziformula = ~ 1)

}
\seealso{
\code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}}, \code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}},
\code{\link[=countGLM]{countGLM()}}, \code{\link[pscl:zeroinfl]{pscl::zeroinfl()}}
}
