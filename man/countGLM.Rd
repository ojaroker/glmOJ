% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countGLM.R
\name{countGLM}
\alias{countGLM}
\title{Fit and compare count regression models}
\usage{
countGLM(
  formula,
  data,
  ziformula = NULL,
  decide = "BIC",
  maxit = NULL,
  families = c("poisson", "negbin", "tweedie"),
  ...
)
}
\arguments{
\item{formula}{A model formula for the count component (e.g. \code{y ~ x1 + x2}).
The response must be non-negative.}

\item{data}{A data frame containing the variables in \code{formula} (and
\code{ziformula} if provided).}

\item{ziformula}{A one-sided formula for the zero-inflation component passed
to \code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}}, \code{\link[=zeroinflNegbinGLM]{zeroinflNegbinGLM()}}, and
\code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}} when they are needed. When \code{NULL} (default), the
same right-hand side as \code{formula} is used.}

\item{decide}{Character string specifying the model-selection criterion.
One of \code{"BIC"} (default), \code{"AIC"}, \code{"LogLik"} (log-likelihood, higher
is better), or \code{"McFadden"} (McFadden pseudo-R², higher is better).
Matching is case-insensitive.}

\item{maxit}{Optional integer; maximum optimizer/IWLS iterations. When
non-\code{NULL}, forwarded as the \code{maxit} argument to each underlying fitter
(\code{\link[=poissonGLM]{poissonGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}}, \code{\link[=tweedieGLM]{tweedieGLM()}}, and the ZI counterparts),
which translate it into the appropriate backend \code{control} object. A
single value is applied across every model family.}

\item{families}{Character vector naming which base families to fit. Must
be a subset of \code{c("poisson", "negbin", "tweedie")}. Defaults to all
three. Each family's zero-inflated counterpart is fitted conditionally
on its base model passing zero-inflation detection (as before). Use
this to skip slow Tweedie / glmmTMB fits or to restrict comparison to a
specific subset. The quasi-Poisson fit (produced when the Poisson fit
shows a constant-overdispersion signature) requires \code{"poisson"} to be
included.}

\item{...}{Additional arguments passed to each individual model fitter.}
}
\value{
An object of class \code{"countGLM"}, a list with:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{fits}}{A named list of successfully fitted model objects. Base
models (\code{poisson}, \code{negbin}, \code{tweedie}) are always attempted.
\code{zeroinfl_poisson}, \code{zeroinfl_negbin}, and \code{zeroinfl_tweedie} are
fitted only when the DHARMa zero-inflation test flags their base
model (p < 0.05). Any model that failed to converge is omitted. Base
model fits include \code{diagnostics$zi_test} populated from the DHARMa
test.}
\item{\code{aic_table}}{A named numeric vector of AICs, sorted ascending.}
\item{\code{bic_table}}{A named numeric vector of BICs, sorted ascending.}
\item{\code{metric_table}}{A named numeric vector of the selection metric
values (the criterion named by \code{decide}), sorted best-first.}
\item{\code{decide}}{The normalised (lower-case) name of the selection
criterion actually used.}
\item{\code{best_model}}{Character name of the model selected by \code{decide}.}
\item{\code{recommendation}}{A plain-language character string explaining
the selection, including the criterion value, dispersion context, and
zero-inflation test results.}
\item{\code{vif}}{A data frame of generalized variance inflation factors
(GVIF; Fox & Monette 1992) for the main-effect predictors in
\code{formula} (interaction and polynomial terms are excluded). Columns
are \code{GVIF}, \code{Df}, and \code{GVIF^(1/(2*Df))} (the degrees-of-freedom
adjusted scalar comparable to a conventional VIF). For single-df
terms (continuous predictors and two-level factors), \code{GVIF} equals
the usual VIF. \code{NULL} when fewer than two main-effect terms are
present. A warning is issued when any term's \code{GVIF^(1/(2*Df))}
exceeds \code{sqrt(5)} (the GVIF analogue of the \code{VIF > 5} rule of thumb).}
}
}
\description{
Fits three base count regression models (Poisson, negative binomial, and
Tweedie), runs a DHARMa zero-inflation test on each, fits the corresponding
zero-inflated counterpart for any model where zero-inflation is detected,
then selects the best overall model by the metric given in \code{decide}.
}
\details{
\strong{Workflow:} \code{\link[=countGLM]{countGLM()}} fits Poisson, negative binomial, and Tweedie
base models. It then runs a DHARMa simulation test for zero-inflation on
each successful base model. For every family, the zero-inflated counterpart
is fitted only when zero-inflation is detected (p < 0.05) on its base
model. A \code{\link[=quasiPoissonGLM]{quasiPoissonGLM()}} fit is additionally produced when the Poisson
fit shows a dispersion ratio > 1.2 combined with a roughly flat squared
Pearson residual cloud that sits above 1 (the quasi-Poisson signature).
Because quasi-Poisson has no proper likelihood, it is excluded from the
\code{decide} comparison and reported alongside it with \code{NA} AIC/BIC.
All surviving likelihood-based models are compared by \code{decide}.

\strong{Model selection:} The model with the best value of \code{decide} is chosen.
For \code{"AIC"} and \code{"BIC"} the model with the \emph{lowest} value wins; for
\code{"LogLik"} and \code{"McFadden"} the model with the \emph{highest} value wins.
AIC and BIC are always computed and displayed regardless of \code{decide}.
When \code{decide = "McFadden"}, intercept-only null models are fitted for each
family to compute the pseudo-R².

Individual models support \code{print()}, \code{summary()}, and \code{plot()}.
}
\examples{
df <- data.frame(
  y  = c(0L, 1L, 2L, 3L, 5L, 0L, 2L, 4L, 1L, 3L),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
result <- suppressWarnings(countGLM(y ~ x1, data = df))      # default: BIC
result <- suppressWarnings(countGLM(y ~ x1, data = df, decide = "AIC"))
result <- suppressWarnings(countGLM(y ~ x1, data = df, decide = "McFadden"))
print(result)
summary(result)

}
\seealso{
\code{\link[=poissonGLM]{poissonGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}}, \code{\link[=tweedieGLM]{tweedieGLM()}},
\code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}}, \code{\link[=zeroinflNegbinGLM]{zeroinflNegbinGLM()}}, \code{\link[=zeroinflTweedieGLM]{zeroinflTweedieGLM()}}
}
