% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quasiPoissonGLM.R
\name{quasiPoissonGLM}
\alias{quasiPoissonGLM}
\title{Fit a quasi-Poisson regression model}
\usage{
quasiPoissonGLM(formula, data, maxit = NULL, dispersion_threshold = 1.2, ...)
}
\arguments{
\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}). The response must be
a non-negative integer count variable.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{maxit}{Optional integer; maximum IWLS iterations passed through as
\code{control = stats::glm.control(maxit = maxit)}. Ignored when the user
supplies their own \code{control} via \code{...}.}

\item{dispersion_threshold}{Numeric; dispersion ratios above this value
are flagged as overdispersed in the diagnostic plot. Default 1.2.}

\item{...}{Additional arguments passed to \code{\link[stats:glm]{stats::glm()}}.}
}
\value{
An object of class \code{c("quasiPoissonGLM", "countGLMfit")}, a list
with:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{model}}{The underlying \link[stats:glm]{stats::glm} fit object.}
\item{\code{summary}}{The result of \code{summary()} on the fitted model.}
\item{\code{phi}}{The estimated quasi-likelihood dispersion parameter
(\code{summary(fit)$dispersion}).}
\item{\code{coefficients}}{A data frame with columns \code{term}, \code{exp.coef},
\code{lower.95}, \code{upper.95}, \code{p.value}, and \code{stars}. Standard errors (and
hence CIs and p-values) are inflated by \code{sqrt(phi)} relative to a
Poisson fit.}
\item{\code{diagnostics}}{A list with:
\describe{
\item{\code{rqr}}{Numeric vector of randomized quantile residuals,
computed from the Poisson CDF evaluated at the fitted means
(quasi-Poisson shares the Poisson mean structure).}
\item{\code{dispersion_ratio}}{Pearson chi-squared / df.residual, equal
to \code{phi}.}
\item{\code{plot}}{Patchwork ggplot: fitted vs RQR and histo-QQ. The
dispersion-ratio label in the title equals \code{phi}.}
\item{\code{r2_plot}}{Squared Pearson residuals vs fitted values.}
}
}
\item{\code{aic}}{\code{NA_real_}. Quasi-likelihood fits do not have a proper
likelihood, so AIC is undefined.}
\item{\code{bic}}{\code{NA_real_}. BIC is also undefined for quasi-likelihood
fits.}
}
}
\description{
Fits a quasi-Poisson GLM via \code{stats::glm(family = quasipoisson())} and
returns exponentiated coefficients with quasi-likelihood Wald 95\% CIs, the
estimated dispersion parameter (\code{phi}), Pearson dispersion ratio, and
diagnostic plots. Quasi-Poisson shares the Poisson mean structure but
estimates a free scale parameter so that \code{Var(Y) = phi * mu}.
}
\details{
\strong{Coefficient interpretation:} Identical to Poisson regression;
exponentiating a coefficient gives the multiplicative change in the expected
count for a one-unit increase in the predictor. The point estimates match
\code{\link[=poissonGLM]{poissonGLM()}} exactly — only the standard errors differ.

\strong{When to use:} Quasi-Poisson is appropriate when a Poisson fit shows
mild-to-moderate overdispersion that appears \emph{constant} across the range
of fitted values — i.e. the squared Pearson residuals form a roughly flat
cloud with mean \code{phi > 1}, rather than fanning out with the fitted mean
(which would motivate \code{\link[=negbinGLM]{negbinGLM()}}). \code{\link[=countGLM]{countGLM()}} automatically fits
quasi-Poisson when the Poisson dispersion ratio exceeds 1.2 and the r²
vs fitted plot is approximately flat above 1.

\strong{No AIC/BIC:} Because quasi-Poisson is a quasi-likelihood method, AIC
and BIC are not defined and are returned as \code{NA}. Quasi-Poisson therefore
does not participate in the likelihood-based comparison performed by
\code{\link[=countGLM]{countGLM()}}; it is reported alongside the main comparison when flagged.
}
\examples{
df <- data.frame(
  y  = c(0L, 1L, 2L, 3L, 5L, 0L, 2L, 4L, 1L, 3L),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
fit <- quasiPoissonGLM(y ~ x1, data = df)
print(fit)
plot(fit)

}
\seealso{
\code{\link[=poissonGLM]{poissonGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}}, \code{\link[=tweedieGLM]{tweedieGLM()}}, \code{\link[=countGLM]{countGLM()}},
\code{\link[stats:glm]{stats::glm()}}
}
