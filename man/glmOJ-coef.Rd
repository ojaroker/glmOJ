% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-countGLMfit.R
\name{glmOJ-coef}
\alias{glmOJ-coef}
\alias{coef.countGLMfit}
\alias{coef.zeroinflGLMfit}
\alias{coef.countGLM}
\title{Extract model coefficients}
\usage{
\method{coef}{countGLMfit}(object, ...)

\method{coef}{zeroinflGLMfit}(object, component = c("both", "count", "zero"), ...)

\method{coef}{countGLM}(object, ...)
}
\arguments{
\item{object}{A fitted model object of class \code{countGLMfit} or \code{zeroinflGLMfit}.}

\item{...}{Unused.}

\item{component}{For zero-inflated models, \code{"both"} (default) returns the
concatenated vector; \code{"count"} returns only the count component;
\code{"zero"} returns only the zero-inflation component.}
}
\value{
A named numeric vector of exponentiated coefficients.
}
\description{
Returns coefficients on the response scale (i.e. exponentiated linear-
predictor coefficients) as a named numeric vector, matching the convention
of \code{\link[stats:coef]{stats::coef()}}. For zero-inflated models the count and zero-inflation
components are concatenated into a single vector with \code{count_} and \code{zero_}
name prefixes, mirroring \code{\link[pscl:zeroinfl]{pscl::zeroinfl()}}.
}
\details{
Use \code{\link[=coef_table]{coef_table()}} to get the full coefficient table including 95\\% Wald
CIs, p-values, and significance stars.
}
\examples{
df <- data.frame(
  y  = c(0L, 1L, 2L, 3L, 5L, 0L, 2L, 4L, 1L, 3L),
  x1 = c(1.2, -0.4, 0.8, -1.1, 2.0, 0.3, -0.9, 1.5, -0.2, 0.7)
)
fit <- poissonGLM(y ~ x1, data = df)
coef(fit)
coef_table(fit)
}
