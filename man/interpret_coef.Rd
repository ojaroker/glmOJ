% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret-coef.R
\name{interpret_coef}
\alias{interpret_coef}
\title{Interpret a model coefficient in natural language}
\usage{
interpret_coef(model, predictor, component = "count")
}
\arguments{
\item{model}{A fitted model returned by \code{\link[=poissonGLM]{poissonGLM()}}, \code{\link[=negbinGLM]{negbinGLM()}},
\code{\link[=zeroinflPoissonGLM]{zeroinflPoissonGLM()}}, \code{\link[=zeroinflNegbinGLM]{zeroinflNegbinGLM()}}, or \code{\link[=countGLM]{countGLM()}}.
For \code{countGLM}, the best-fitting model is used automatically.}

\item{predictor}{Character; the exact term name as it appears in the
coefficient table (e.g. \code{"pctnonwhite10"}, \code{"EPAregion2"}).}

\item{component}{For zero-inflated models: \code{"count"} (default) for the
count component, or \code{"zero"} for the zero-inflation component. Ignored
for Poisson and negative binomial models.}
}
\value{
The interpretation string, printed to the console and returned
invisibly.
}
\description{
Generates a plain-language interpretation of an exponentiated regression
coefficient: the percent change in the expected count (or rate, if an offset
is present), with a 95\% Wald confidence interval. For zero-inflated models,
the \code{component} argument selects the count or zero-inflation part.
A note is appended whenever the coefficient is not discernibly different
from zero (p > 0.05).
}
